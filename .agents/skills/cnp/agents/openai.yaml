interface:
  display_name: "CNP"
  short_description: "현재 브랜치 변경사항 커밋 후 리모트 푸시 수행"
  default_prompt: "Use $cnp to commit current changes and push the branch."
policy:
  allow_implicit_invocation: true
