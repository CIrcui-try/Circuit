interface:
  display_name: "Review and Fix"
  short_description: "로컬 변경사항 리뷰 후 문제 수정과 push 수행"
  default_prompt: "Use $review-and-fix to review local git changes directly and push fixes."
policy:
  allow_implicit_invocation: true
