interface:
  display_name: "Release"
  short_description: "develop 변경사항으로 release PR 생성"
  default_prompt: "Use $release to create a release branch and PR from develop."
policy:
  allow_implicit_invocation: true
